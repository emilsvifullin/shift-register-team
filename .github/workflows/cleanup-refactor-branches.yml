name: Cleanup merged refactor branches

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cleanup-refactor-branches.yml"

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Checkout main
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Delete merged work branches and self-clean
        shell: bash
        run: |
          set -euo pipefail
          for branch in \
            refactor/cross-platform-foundation \
            refactor/production-quality-final \
            refactor/production-quality-baseline
          do
            git push origin --delete "$branch" || true
          done

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/cleanup-refactor-branches.yml
          git commit -m "chore: finish refactor branch cleanup"
          git push origin HEAD:main
